{
  "_README": {
    "reason": "CREDITS-BUNDLE-HEURISTIC-1 allowlist (helm-side port of the site repo's scripts/original-authorship-bundle-allowlist.json discriminator, same gate name). check-credits-coverage.mjs's BUNDLE_FILE_RE flags any *.bundle.mjs/*.bundle.js filename as vendored third-party by convention. ACCT-INFRA-KERNELS-BUILD-SPEC.md §4.1 mandates that exact suffix, 'exactly the shipped _detmath shape', for ANY shared, inline-consumed kernel composition module, regardless of whether its contents are original or vendored — so the filename alone cannot distinguish the two. This registry is the discriminator: a *.bundle.mjs file is either covered by data/credits-registry.json (genuinely vendored) or listed here with a written reason (genuinely original, authored in-house). A file matching neither is failed by the gate. Every entry carries a mandatory `reason`; an entry without one fails the gate on that ground alone. Keys are repo-relative paths with forward slashes — in this repo the site-authored bundles arrive mirrored at their hub/vendored/ocg/kernels/ paths via the pinned OCG mirror (scripts/vendor.config.json), byte-identical to the site-repo originals listed in the site repo's own allowlist. `_README` is inert — it names no bundle path, so it shields nothing."
  },

  "hub/vendored/ocg/kernels/_ruleversion.bundle.mjs": {
    "reason": "Mirror (scripts/vendor.config.json pin) of the site repo's chaingraph/kernels/_ruleversion.bundle.mjs — ACCT-RULEREG-K-1, original authorship per the site repo's own allowlist entry for this byte-identical file. Original-authorship effective-date / rule-version registry resolver, authored from scratch for this estate per ACCT-INFRA-KERNELS-BUILD-SPEC.md §2. No third-party source, package, or code was copied into it: the closed filer-status enum, the half-open parameter-window resolution, the ISO calendar arithmetic and the bounded in-guest slice-digest assertion are all written for the originating row. Its pure-JS UTF-8 encoder and SHA-256 are the estate's own validated in-house construction (ART595-ART590-UTF8-FIX-1), reused rather than re-authored, and likewise not third-party. Named `_ruleversion.bundle.mjs` (not `_ruleversion.mjs`) because ACCT-INFRA-KERNELS-BUILD-SPEC.md §4.1 mandates the shipped `_detmath.bundle.mjs` shape verbatim for any shared, inline-consumed kernel bundle — the filename convention marks 'reuse vehicle, always inlined, never imported at runtime', independent of authorship."
  },

  "hub/vendored/ocg/kernels/_amort.bundle.mjs": {
    "reason": "Mirror (scripts/vendor.config.json pin) of the site repo's chaingraph/kernels/_amort.bundle.mjs — ACCT-AMORT-K-1 (PR #1290), original authorship per the site repo's own allowlist entry for this byte-identical file. Original-authorship deterministic amortization-schedule kernel, authored from scratch for this estate against ISDA 2006 Definitions §4.16 (day-count conventions) and the effective-interest method, per ACCT-INFRA-KERNELS-BUILD-SPEC.md §1. No third-party source, package, or code was copied into it. Named `_amort.bundle.mjs` (not `_amort.mjs`) because ACCT-INFRA-KERNELS-BUILD-SPEC.md §4.1 mandates the shipped `_detmath.bundle.mjs` shape verbatim for any shared, inline-consumed kernel bundle — the filename convention marks 'reuse vehicle, always inlined, never imported at runtime', independent of authorship."
  },

  "hub/vendored/ocg/kernels/_dtree.bundle.mjs": {
    "reason": "Mirror (scripts/vendor.config.json pin) of the site repo's chaingraph/kernels/_dtree.bundle.mjs — ACCT-DTREE-K-1, original authorship per the site repo's own allowlist entry for this byte-identical file. Original-authorship declarative decision-tree interpreter, authored from scratch for this estate per ACCT-INFRA-KERNELS-BUILD-SPEC.md §3 (closed operator set, citation-gated nodes, tree_digest fail-closed via a hand-rolled guest-safe SHA-256 + canonicalizer). No third-party source, package, or code was copied into it — the hand-rolled SHA-256/UTF-8-encoder construction reuses this estate's own already-proven pattern (art-199/200/206/210/280/584/620), not an external library. Named `_dtree.bundle.mjs` (not `_dtree.mjs`) because ACCT-INFRA-KERNELS-BUILD-SPEC.md §4.1 mandates the shipped `_detmath.bundle.mjs` shape verbatim for any shared, inline-consumed kernel bundle — the filename convention marks 'reuse vehicle, always inlined, never imported at runtime', independent of authorship."
  }
}
