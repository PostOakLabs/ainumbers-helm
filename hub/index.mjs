#!/usr/bin/env node
// helmd placeholder entrypoint. Real daemon (config, loopback API, doctor
// self-check) lands in HELM-H1 — this stub exists so the SEA build pipeline
// has something to blob from day one.
console.log("helmd placeholder — see HELM-H1 for the real daemon");
